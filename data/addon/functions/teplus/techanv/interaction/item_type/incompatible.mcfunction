#CEitem is removed, so the Technical Anivl ignores the combination
#ComError is added, so the Technical Anvil properly updates it's UI

# Do not change anithing in this file, just make sure to use it if you want
# a certain item to not be compatible with certain enchantments in "addon:teplus/techanv/interaction/item_type/item_base" function
tag @s remove CEitm
tag @s add ComError